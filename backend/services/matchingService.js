/**
 * matchingService.js
 *
 * Service layer for matching logic.
 * Currently matching logic lives in matchingController.js.
 * Future refactor: move buildMatchResult, calculateSkillScore, etc. here
 * to keep controllers thin and services testable in isolation.
 */

module.exports = {};
