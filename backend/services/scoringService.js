/**
 * scoringService.js
 *
 * Service layer for scoring logic.
 * Currently scoring logic lives in matchingController.js via calculateSkillScore().
 * Future refactor: move calculateSkillScore here and export it for use in
 * both matching and any future scoring-specific endpoints.
 */

module.exports = {};
